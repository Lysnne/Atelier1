onst [age, setAge] = useState(0)// useState useeffect march en couple fait le getter et setter si je veux le modifier on ajoute du set 

useEffect c'est un function , elle permet de faire de cole http

response c'estun variable pour mettre les donnes pour le mettre dans le tabUsers

abUsers pour boucle et on utilise map et ligne c'est un variable 

  tabUsers.map(ligne, i) =>
                            tr key = {i} il faut creer un kle

difference :
axios il faut l'installer et fetch est native 

on peut pas comparer useEffect entre useState